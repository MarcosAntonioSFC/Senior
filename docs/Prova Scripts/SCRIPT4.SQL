SELECT COUNT(IDNF) TOTNF,
       DATACADASTRO
FROM EXAME_NF NF
WHERE EXISTS(
              SELECT NULL
              FROM EXAME_ITEMNF NFI
              WHERE NF.IDNF = NFI.IDNF
                AND NFI.VALOR < 50)
GROUP BY DATACADASTRO;